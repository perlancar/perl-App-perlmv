#!/usr/bin/perl

# workaround for dzil
package main;

use strict;
use warnings;
use FindBin '$Bin';
use lib "$Bin/../lib";
use App::perlmv;

my $pmv = App::perlmv->new;
$pmv->{ 'mode' } = 'link';
$pmv->run;

1;
